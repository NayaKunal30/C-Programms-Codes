//GARBAGE VALUE : the leftover value from the previous program that is contained in the memory block allocated to the variable.
#include<stdio.h>
int main()
{
    int x;
    printf("%d",x);
    return 0 ;
}